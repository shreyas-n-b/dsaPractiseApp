#ifndef SORTING_H
#define SORTING_H

#include <vector>

// ------------------------------------
// Public Sorting Function Declarations
// ------------------------------------

// Bubble Sort
void bubbleSort(std::vector<int>& arr);

// Merge Sort (single-parameter version used in main)
void mergeSort(std::vector<int>& arr);

// Quick Sort (single-parameter version used in main)
void quickSort(std::vector<int>& arr);

#endif // SORTING_H
